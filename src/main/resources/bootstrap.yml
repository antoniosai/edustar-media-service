spring:
  profiles:
    active: default
  application:
    name: media
  output:
    ansi:
      enabled: ALWAYS
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8090
      enabled: true
  devtools:
    add-properties:
      false